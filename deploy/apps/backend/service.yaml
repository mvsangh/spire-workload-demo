apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: demo
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - name: envoy-http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: backend
