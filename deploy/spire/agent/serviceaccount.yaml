# SPIRE Agent ServiceAccount
# Identity for SPIRE agent pods

apiVersion: v1
kind: ServiceAccount
metadata:
  name: spire-agent
  namespace: spire-system
  labels:
    app.kubernetes.io/name: spire-agent
    app.kubernetes.io/component: agent
