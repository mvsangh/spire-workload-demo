# SPIRE Server ServiceAccount
# Used by SPIRE server pods for Kubernetes API access

apiVersion: v1
kind: ServiceAccount
metadata:
  name: spire-server
  namespace: spire-system
  labels:
    app.kubernetes.io/name: spire-server
    app.kubernetes.io/component: server
